apiVersion: 1
datasources:
  - name: Prometheus
    # Fixed uid so provisioned dashboards reference the datasource deterministically.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
